// Server-side Supabase instance for API routes
export { createClient } from './server'

// Client-side Supabase instance (if needed separately)
export { createClient as createBrowserClient } from './client'